apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config

data:
  local.yaml: |
    env: "dev"

    storage-path: "./storage/storage.db"

    http_server:
      address: "0.0.0.0:8080"
      timeout: 4s
      idle_timeout: 60s
